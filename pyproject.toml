[project]
name = "anteater-api-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "atile4", email = "atticus1467@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.1.1",
    "requests>=2.34.2",
]

[project.scripts]
anteater-api-mcp = "anteater_api_mcp:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
